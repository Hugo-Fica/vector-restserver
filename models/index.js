const Vector = require('./vector.models');
const Role = require('./role.models');
const User = require('./user.models');
const VectorValue = require('./valueVector.models');
const AreaVector = require('./areaVector.models');
module.exports = {
  AreaVector,
  Vector,
  Role,
  User,
  VectorValue,
};
